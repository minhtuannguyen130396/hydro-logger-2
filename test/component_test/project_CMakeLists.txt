# Component Test Program - Project-level CMakeLists.txt
#
# Usage: Copy this file to the project root CMakeLists.txt to build
# the test firmware instead of the main application.
#
#   cp docs/test_case/CMakeLists.txt CMakeLists.txt
#   idf.py build
#   idf.py -p COMx flash monitor
#
# To restore the main application:
#   git checkout CMakeLists.txt
#   idf.py fullclean && idf.py build

cmake_minimum_required(VERSION 3.16)

# Override EXTRA_COMPONENT_DIRS so ESP-IDF picks up test sources
# alongside the existing board/modules/services drivers from main/
set(EXTRA_COMPONENT_DIRS "")

include($ENV{IDF_PATH}/tools/cmake/project.cmake)
project(hydro-logger-test)
