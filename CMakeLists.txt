cmake_minimum_required(VERSION 3.16)

# ESP-IDF project build file.
include($ENV{IDF_PATH}/tools/cmake/project.cmake)
project(esp32_water_level CXX)

# Note: set C++ standard in main/CMakeLists.txt via target_compile_options.
